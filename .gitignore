# dependencies
node_modules/

# native build output (regenerated by `expo prebuild` / gradle)
app/android/
app/ios/
app/.expo/
**/build/
**/.gradle/
**/.cxx/
*.dex

# build artefacts
*.apk
*.aab

# signing material and secrets — never commit these
app/credentials/
*.keystore
*.jks
*.env
!*.env.example
app/src/data/defaults.local.ts

# generated presentation renders
*.pptx.bak

# os / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# superseded deck from the Windows-era design — kept on disk, not published
MediBloom-Pitch-Deck.pptx

# pitch deck and standalone doc — kept on disk for judges, not published to the repo
MediBloom-Pitch.pptx
MediBloom-Documentation.pdf
